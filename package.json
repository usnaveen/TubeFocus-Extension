{
    "name": "tubefocus-extension",
    "version": "1.0.0",
    "description": "TubeFocus Youtube Extension",
    "main": "background.js",
    "scripts": {
        "build:ml": "esbuild ml_service.js --bundle --outfile=dist/ml_worker.bundle.js --format=iife --platform=browser"
    },
    "dependencies": {
        "@xenova/transformers": "^2.17.2"
    },
    "devDependencies": {
        "esbuild": "^0.20.2"
    }
}